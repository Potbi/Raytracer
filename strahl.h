#ifndef STRAHL_H
#define STRAHL_H

#include "plan.h"

class Strahl {
public:
    // Member-Variablen
    TVektor ursprung;
    TVektor richtung;

    // Konstruktoren
    Strahl();
    Strahl(TVektor ursprung, TVektor richtung);

    // Member-Funktionen

private:

};
#endif